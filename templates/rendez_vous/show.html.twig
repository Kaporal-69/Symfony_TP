{% extends 'base.html.twig' %}

{% block title %}Rendez-vous{% endblock %}

{% block body %}
    <h1>Rendez-vous</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ rendez_vou.id }}</td>
            </tr>
            <tr>
                <th>Jour</th>
                <td>{{ rendez_vou.jour ? rendez_vou.jour|date('Y-m-d') : '' }}</td>
            </tr>
            <tr>
                <th>Horaire</th>
                <td>{{ rendez_vou.horaire }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('rendez_vous_index') }}">Retour à la liste</a>

    <a href="{{ path('rendez_vous_edit', {'id': rendez_vou.id}) }}">Modifier</a>

    {{ include('rendez_vous/_delete_form.html.twig') }}
{% endblock %}
